using UnityEngine;
using Abstract_Ammonites;
public class simple_tile : MonoBehaviour
{
    // Start is called before the first frame update
    public tile_base contents;
    public bool is_spawner;
}